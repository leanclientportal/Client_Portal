'use client';

import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export function WhatsappNotificationSettings() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>WhatsApp Notification</CardTitle>
        <CardDescription>
          Configure how you receive WhatsApp notifications.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>WhatsApp notification settings will be available soon.</p>
      </CardContent>
    </Card>
  );
}
